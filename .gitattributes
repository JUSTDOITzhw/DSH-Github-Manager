# Normalise line endings on the way in, LF on the way out.
# Windows-only shells need CRLF; everything else (including the .mjs sources)
# must stay LF or Linux/macOS users get `bad interpreter` / parse surprises.
* text=auto eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
